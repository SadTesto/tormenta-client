import React from 'react';
import { Row, Col, Card } from 'antd';

const Matches = () => (
    <Row>
        <Col span={24} md={10}>
            <Card bordered={true}>
                Pagina in costruzione.
            </Card>
        </Col>
    </Row>
);

export default Matches;